[alias]
    hist = log --pretty=format:\"%h %ad | %s [%an]\" --graph --date=short
    today = !git hist --since="0am"
    ci = commit -a -m
    co = checkout
    st = status
[core]
    autocrlf=true
    safecrlf=false
